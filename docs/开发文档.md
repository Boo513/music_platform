# 在线3D动态背景音乐平台 — 开发文档

## 1. 技术栈

| 层 | 技术 | 版本 |
|---|------|------|
| 前端框架 | React + TypeScript + Vite | React 19, Vite 8 |
| 3D 引擎 | react-three-fiber + Three.js | r3f 9, Three r184 |
| 状态管理 | Zustand | 5.x |
| UI 样式 | 手写 CSS（自定义工具类 + 页面独立样式） | — |
| UI 组件 | Radix UI 原语 + Framer Motion 动画 | Radix latest, Framer Motion 12 |
| 后端框架 | Spring Boot | 3.5.14 |
| ORM | MyBatis | 3.x (mybatis-spring-boot-starter) |
| 认证 | JWT (jjwt) + BCrypt | jjwt 0.12, spring-security-crypto |
| 数据库 | MySQL | 8.0+ |
| 构建工具 | Maven | 3.6.3 |
| JDK | Java | 17 |

---

## 2. 后端三层架构

### 2.1 架构总览

```
┌─────────────────────────────────────────────────────────┐
│  请求流向：Controller → Service → Mapper → Database      │
│  响应流向：Controller ← Service ← Mapper ← Database      │
│                                                         │
│  ┌──────────────┐    ┌──────────────┐    ┌────────────┐ │
│  │  Controller  │───▶│   Service    │───▶│   Mapper   │ │
│  │  控制层       │◀───│   业务层      │◀───│   持久层    │ │
│  │              │    │              │    │            │ │
│  │ · 接收请求    │    │ · 业务逻辑    │    │ · SQL 映射  │ │
│  │ · 参数校验    │    │ · 事务管理    │    │ · 数据库交互 │ │
│  │ · 返回响应    │    │ · 数据组装    │    │ · CRUD 操作 │ │
│  └──────────────┘    └──────────────┘    └────────────┘ │
│                          │                              │
│                          ▼                              │
│                    ┌──────────────┐                     │
│                    │    Entity    │                     │
│                    │   实体层      │                     │
│                    │              │                     │
│                    │ · 数据库映射  │                     │
│                    │ · 纯数据对象  │                     │
│                    └──────────────┘                     │
└─────────────────────────────────────────────────────────┘
```

### 2.2 各层职责与约束

| 层 | 职责 | 禁止 | 依赖方向 |
|---|------|------|----------|
| **Controller** | 接收HTTP请求、校验参数、调用Service、封装响应 | 不包含业务逻辑、不直接调用Mapper | → Service |
| **Service** | 业务逻辑编排、事务管理、数据校验、DTO转换 | 不处理HTTP请求/响应、不直接操作HttpServletRequest | → Mapper |
| **Mapper** | SQL映射、数据库CRUD、结果集映射为Entity | 不包含业务逻辑、不调用其他Mapper | → Entity |
| **Entity** | 纯数据对象，与数据库表字段一一映射 | 不包含业务方法（仅getter/setter） | 无 |

### 2.3 调用链路示例：歌曲播放

```
用户请求: GET /api/songs/1/stream
              │
    ┌─────────▼─────────┐
    │ SongController    │  ← 控制层：接收请求，从SecurityContext获取userId
    │ .stream(id,       │
    │  request,         │
    │  Authentication)  │
    └─────────┬─────────┘
              │ 调用
    ┌─────────▼─────────┐
    │ SongService       │  ← 业务层：校验歌曲是否存在、构建ResourceRegion、
    │ .getStreamData    │    处理Range请求头、返回ResourceRegion
    │ (id, httpHeaders) │
    └─────────┬─────────┘
              │ 调用
    ┌─────────▼─────────┐
    │ SongMapper        │  ← 持久层：SELECT * FROM songs WHERE id = ?
    │ .selectById(id)   │    返回Song实体
    └─────────┬─────────┘
              │ 返回
    ┌─────────▼─────────┐
    │ Song Entity       │  ← 实体层：id, title, file_path, duration...
    └───────────────────┘
```

### 2.4 完整调用示例：上传歌曲

```
POST /api/songs/upload (multipart/form-data)
              │
    ┌─────────▼──────────────────────────────────────┐
    │ SongController.upload()                        │
    │  1. @Valid 校验 title/artist/style/mood 非空    │
    │  2. 从 Authentication 获取 userId               │
    │  3. 调用 songService.upload(...)                │
    │  4. 将返回的 Song 转为 ApiResponse 返回          │
    └─────────┬──────────────────────────────────────┘
              │
    ┌─────────▼──────────────────────────────────────┐
    │ SongService.upload(title, artist, style,        │
    │                    mood, file, userId)          │
    │  1. 生成 UUID 文件名 → uploads/audio/uuid.mp3    │
    │  2. Files.copy() 写入磁盘                       │
    │  3. 获取文件时长（JAudioTagger解析MP3）           │
    │  4. 构建 Song 实体，调用 songMapper.insert(song) │
    │  5. 返回完整的 Song 对象                         │
    │  @Transactional ← 文件写入+数据库插入原子操作     │
    └─────────┬──────────────────────────────────────┘
              │
    ┌─────────▼──────────────────────────────────────┐
    │ SongMapper.insert(Song song)                   │
    │  INSERT INTO songs (title, artist, file_path,   │
    │    duration, style, mood, uploader_id)          │
    │  VALUES (#{title}, #{artist}, ...)              │
    └────────────────────────────────────────────────┘
```

### 2.5 包结构对应

```
com.musicplatform/
├── controller/    ← 控制层：@RestController, @RequestMapping
├── service/       ← 业务层：@Service, @Transactional
├── mapper/        ← 持久层：@Mapper (MyBatis接口)
├── entity/        ← 实体层：纯POJO，对应数据库表
├── dto/           ← 数据传输对象：Request/Response DTO
├── config/        ← 配置类：Security, CORS, MyBatis
├── security/      ← 安全组件：JWT, Filter
└── exception/     ← 异常处理：全局异常处理器
```

---

## 3. 项目目录结构

```
在线音乐播放平台/
├── frontend/                          # React + TypeScript (Vite)
│   ├── public/
│   ├── src/
│   │   ├── api/                       # Axios 请求封装
│   │   │   ├── client.ts              # Axios 实例 + 拦截器
│   │   │   ├── auth.ts                # 登录/注册 API
│   │   │   ├── songs.ts               # 歌曲 CRUD API
│   │   │   ├── playlists.ts           # 歌单 API
│   │   │   ├── favorites.ts           # 收藏 API
│   │   │   └── history.ts             # 播放历史 API
│   │   ├── components/
│   │   │   ├── ui/                    # Radix UI 组件（avatar, button, card, dialog, input, scroll-area, tabs）
│   │   │   ├── layout/                # 布局组件
│   │   │   │   ├── AppLayout.tsx      # 全局布局（路由出口 + 底部播放器栏）
│   │   │   │   ├── PlayerBar.tsx      # 底部播放器栏（唱片+信息+进度条+音量+播放按钮，内联样式）
│   │   │   │   ├── PlayerBarRomantic.tsx  # 登录页装饰性播放器栏（粉金主题）
│   │   │   │   ├── TopIcons.tsx       # 左上角图标栏（🔊音量 ♡喜欢 ⚙设置）
│   │   │   │   ├── RightControls.tsx  # 右侧垂直控制栏（□面板 +放大 -缩小 ⛶全屏，白底圆形）
│   │   │   │   └── RadioPanel.tsx     # 左侧电台信息面板（缩略图+歌曲信息+停止按钮）
│   │   │   ├── auth/
│   │   │   │   └── LoginForm.tsx      # 登录/注册切换表单
│   │   │   ├── home/                  # 首页组件
│   │   │   │   ├── UIOverlay.tsx      # 首页 UI 覆盖层（英雄文字 + 场景列表 + 音乐列表）
│   │   │   │   ├── SceneList.tsx      # 场景选择器（6 种场景 + 搜索）
│   │   │   │   ├── MusicList.tsx      # 音乐列表（流派标签 + 演示歌曲）
│   │   │   │   └── Shockwave.tsx      # 场景切换冲击波 CSS 动画
│   │   │   ├── upload/                # 上传页组件
│   │   │   │   ├── FormPanel.tsx      # 上传表单（标题/艺术家/风格/情绪标签）
│   │   │   │   ├── DropZone.tsx       # MP3 拖拽区（格式校验 + 50MB 限制）
│   │   │   │   └── EcgMonitor.tsx     # ECG 波形 Canvas 动画（PQRST 波段）
│   │   │   ├── scenes/                # react-three-fiber 3D 场景
│   │   │   │   ├── Scene3D.tsx        # 登录页 3D 场景（鼠标视差 + 浪漫环形结）
│   │   │   │   ├── RomanticShape.tsx  # 登录页粉金环形结（MeshPhysicalMaterial）
│   │   │   │   ├── HomeScene3D.tsx    # 首页粒子隧道场景
│   │   │   │   ├── ParticleTunnel.tsx # 6000 粒子圆柱隧道（速度/颜色动态变化）
│   │   │   │   ├── CityScene.tsx      # 抽象城市 3D 场景（旋转几何体 + 网格地面）
│   │   │   │   ├── StarryGalaxy.tsx   # 旋转星群（紫色发光球体）
│   │   │   │   ├── SceneEngine.tsx    # 场景路由器（当前返回 CityScene）
│   │   │   │   ├── SceneBanner.tsx    # 场景横幅（Stars + 场景选择面板）
│   │   │   │   └── LoginBackground.tsx # 简单登录背景（Stars + 弱环境光）
│   │   │   ├── settings/
│   │   │   │   └── SettingsPanel.tsx  # 右侧滑出设置面板（场景主题 + 效果开关）
│   │   │   ├── modals/
│   │   │   │   ├── EditProfileModal.tsx    # 编辑个人资料玻璃态弹窗
│   │   │   │   └── CreatePlaylistModal.tsx # 创建歌单玻璃态弹窗
│   │   │   └── shared/                # 共享 UI 组件
│   │   │       ├── GlassButton.tsx    # 圆形玻璃态按钮
│   │   │       ├── NeonButton.tsx     # 全宽霓虹渐变按钮
│   │   │       ├── GlassInput.tsx     # 玻璃态输入框
│   │   │       ├── TagChip.tsx        # 可选标签（emoji + 文字）
│   │   │       ├── SongCard.tsx       # 歌曲卡片
│   │   │       ├── SongRow.tsx        # 歌曲行条目
│   │   │       └── EmptyState.tsx     # 空状态占位
│   │   ├── hooks/
│   │   │   └── useAudioPlayer.ts      # <audio> 元素封装 Hook（自动下一曲 + 历史记录）
│   │   ├── stores/                    # Zustand 状态管理（5 个 store）
│   │   │   ├── authStore.ts           # 用户认证状态
│   │   │   ├── playerStore.ts         # 播放器全局状态（队列/播放模式/音量）
│   │   │   ├── useStore.ts            # 首页状态（鼠标位置/速度/场景/冲击波）
│   │   │   ├── useUploadStore.ts      # 上传状态（文件/进度/表单字段）
│   │   │   └── useMouseStore.ts       # 登录页鼠标跟踪（3D 视差）
│   │   ├── pages/                     # 5 个页面
│   │   │   ├── HomePage.tsx           # 首页（3D 粒子隧道 + 场景/音乐选择器）
│   │   │   ├── LoginPage.tsx          # 登录/注册（3D 浪漫场景 + 玻璃态表单）
│   │   │   ├── PlayPage.tsx           # 全屏播放页（城市 3D 场景 + 灯塔 + 雪花粒子）
│   │   │   ├── UploadPage.tsx         # 上传页（赛博朋克风格 + ECG 监视器）
│   │   │   └── ProfilePage.tsx        # 个人主页（4 标签页：歌单/上传/历史/收藏）
│   │   ├── styles/                    # 页面独立样式
│   │   │   ├── login.css              # 登录页样式（粉金主题）
│   │   │   ├── home.css               # 首页样式
│   │   │   └── upload.css             # 上传页样式（霓虹绿主题）
│   │   ├── types/
│   │   │   └── index.ts               # 全局 TypeScript 类型 + 枚举常量
│   │   ├── utils/
│   │   │   └── format.ts              # formatTime 时间格式化
│   │   ├── lib/
│   │   │   └── utils.ts               # cn() 类名合并工具
│   │   ├── App.tsx                    # 路由配置（lazy-loaded + AnimatePresence 过渡）
│   │   ├── main.tsx                   # 入口
│   │   └── index.css                  # 手写全局 CSS（500+ 行工具类 + 玻璃态/霓虹效果）
│   ├── index.html
│   ├── package.json
│   ├── tsconfig.json
│   └── vite.config.ts
│
├── backend/                           # Spring Boot 3.5.14
│   ├── src/main/java/com/musicplatform/
│   │   ├── MusicPlatformApplication.java
│   │   ├── config/
│   │   │   ├── SecurityConfig.java        # Spring Security + JWT 过滤器
│   │   │   ├── WebMvcConfig.java          # 跨域 CORS + 静态资源配置
│   │   │   └── MyBatisConfig.java         # MyBatis 分页插件配置
│   │   ├── controller/
│   │   │   ├── AuthController.java        # /api/auth/*
│   │   │   ├── UserController.java        # /api/user/*
│   │   │   ├── SongController.java        # /api/songs/*
│   │   │   ├── PlaylistController.java    # /api/playlists/*
│   │   │   ├── FavoriteController.java    # /api/favorites/*
│   │   │   └── HistoryController.java     # /api/history/*
│   │   ├── service/
│   │   │   ├── AuthService.java
│   │   │   ├── UserService.java
│   │   │   ├── SongService.java
│   │   │   ├── PlaylistService.java
│   │   │   ├── FavoriteService.java
│   │   │   └── HistoryService.java
│   │   ├── mapper/
│   │   │   ├── UserMapper.java
│   │   │   ├── SongMapper.java
│   │   │   ├── PlaylistMapper.java
│   │   │   ├── PlaylistSongMapper.java
│   │   │   ├── FavoriteMapper.java
│   │   │   └── HistoryMapper.java
│   │   ├── entity/
│   │   │   ├── User.java
│   │   │   ├── Song.java
│   │   │   ├── Playlist.java
│   │   │   ├── PlaylistSong.java
│   │   │   ├── Favorite.java
│   │   │   └── PlayHistory.java
│   │   ├── dto/
│   │   │   ├── LoginRequest.java
│   │   │   ├── RegisterRequest.java
│   │   │   ├── SongUploadRequest.java
│   │   │   ├── PlaylistCreateRequest.java
│   │   │   └── ApiResponse.java           # 统一响应体
│   │   ├── security/
│   │   │   ├── JwtTokenProvider.java      # JWT 生成/验证
│   │   │   ├── JwtAuthenticationFilter.java
│   │   │   └── UserPrincipal.java         # Security UserDetails
│   │   └── exception/
│   │       ├── GlobalExceptionHandler.java
│   │       ├── BusinessException.java
│   │       └── ErrorCode.java
│   ├── src/main/resources/
│   │   ├── application.yml
│   │   ├── mapper/                        # MyBatis XML 映射文件
│   │   │   ├── UserMapper.xml
│   │   │   ├── SongMapper.xml
│   │   │   ├── PlaylistMapper.xml
│   │   │   ├── PlaylistSongMapper.xml
│   │   │   ├── FavoriteMapper.xml
│   │   │   └── HistoryMapper.xml
│   │   └── schema.sql                     # 数据库初始化 DDL
│   ├── src/test/java/com/musicplatform/
│   └── pom.xml
│
├── uploads/                               # MP3 文件 + 封面图片存储
│   ├── audio/                             # MP3 文件
│   └── covers/                            # 封面图片
│
├── docs/
│   ├── 开发文档.md                         # 本文档
│   └── specs/
│       └── 2026-05-08-在线音乐播放平台-设计文档.md
│
└── preview/                               # 原型预览（纯 HTML，开发时参考）
    ├── spring-blossom.html
    ├── summer-beach.html
    └── india-radio.html
```

---

## 4. 数据库设计

### 4.1 DDL

```sql
CREATE DATABASE music_platform DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE music_platform;

-- 用户表
CREATE TABLE users (
    id          BIGINT AUTO_INCREMENT PRIMARY KEY,
    username    VARCHAR(50)  NOT NULL UNIQUE,
    password    VARCHAR(255) NOT NULL COMMENT 'BCrypt加密',
    nickname    VARCHAR(100),
    avatar      VARCHAR(500) COMMENT '头像URL',
    created_at  DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at  DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB;

-- 歌曲表
CREATE TABLE songs (
    id          BIGINT AUTO_INCREMENT PRIMARY KEY,
    title       VARCHAR(200) NOT NULL,
    artist      VARCHAR(200) NOT NULL DEFAULT '未知艺术家',
    file_path   VARCHAR(500) NOT NULL COMMENT 'MP3文件路径（相对于uploads/audio/）',
    cover_path  VARCHAR(500) COMMENT '封面图路径',
    duration    INT NOT NULL DEFAULT 0 COMMENT '时长（秒）',
    style       VARCHAR(20) NOT NULL COMMENT '风格: rock/pop/classical/electronic/folk/jazz/hiphop/rnb',
    mood        VARCHAR(20) NOT NULL COMMENT '情绪: happy/sad/calm/excited/romantic/melancholy',
    uploader_id BIGINT NOT NULL,
    play_count  INT NOT NULL DEFAULT 0,
    created_at  DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (uploader_id) REFERENCES users(id),
    INDEX idx_style (style),
    INDEX idx_mood (mood),
    INDEX idx_style_mood (style, mood),
    INDEX idx_play_count (play_count DESC)
) ENGINE=InnoDB;

-- 歌单表
CREATE TABLE playlists (
    id          BIGINT AUTO_INCREMENT PRIMARY KEY,
    name        VARCHAR(200) NOT NULL,
    description TEXT,
    cover       VARCHAR(500),
    user_id     BIGINT NOT NULL,
    is_public   TINYINT(1) NOT NULL DEFAULT 1,
    created_at  DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at  DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id)
) ENGINE=InnoDB;

-- 歌单-歌曲关联表
CREATE TABLE playlist_songs (
    id          BIGINT AUTO_INCREMENT PRIMARY KEY,
    playlist_id BIGINT NOT NULL,
    song_id     BIGINT NOT NULL,
    sort_order  INT NOT NULL DEFAULT 0,
    added_at    DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (playlist_id) REFERENCES playlists(id) ON DELETE CASCADE,
    FOREIGN KEY (song_id) REFERENCES songs(id) ON DELETE CASCADE,
    UNIQUE KEY uk_playlist_song (playlist_id, song_id)
) ENGINE=InnoDB;

-- 收藏表
CREATE TABLE favorites (
    id          BIGINT AUTO_INCREMENT PRIMARY KEY,
    user_id     BIGINT NOT NULL,
    song_id     BIGINT NOT NULL,
    created_at  DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (song_id) REFERENCES songs(id),
    UNIQUE KEY uk_user_song (user_id, song_id)
) ENGINE=InnoDB;

-- 播放历史表
CREATE TABLE play_history (
    id          BIGINT AUTO_INCREMENT PRIMARY KEY,
    user_id     BIGINT NOT NULL,
    song_id     BIGINT NOT NULL,
    played_at   DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (song_id) REFERENCES songs(id),
    INDEX idx_user_played (user_id, played_at DESC)
) ENGINE=InnoDB;
```

### 4.2 风格与情绪枚举

| 字段 | 枚举值 |
|------|--------|
| style | rock, pop, classical, electronic, folk, jazz, hiphop, rnb |
| mood | happy, sad, calm, excited, romantic, melancholy |

---

## 5. 后端 API 设计

### 5.1 统一响应格式

**通用响应体：**

```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

**分页响应体：**

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "records": [],
    "total": 100,
    "page": 1,
    "size": 20,
    "pages": 5
  }
}
```

**错误响应体：**

```json
{
  "code": 401,
  "message": "Token已过期或无效",
  "data": null
}
```

### 5.2 HTTP 状态码规范

| 场景 | code |
|------|------|
| 请求成功 | 200 |
| 创建资源成功 | 200（不使用 201） |
| 参数校验失败 | 400 |
| 未认证（无 Token / Token 无效） | 401 |
| 无权限（非资源所有者） | 403 |
| 资源不存在 | 404 |
| 资源冲突（重复收藏/重复用户名） | 409 |
| 服务器内部错误 | 500 |

### 5.3 全局错误码

| 错误码 | 说明 |
|--------|------|
| `USERNAME_EXISTS` | 用户名已被注册 |
| `INVALID_CREDENTIALS` | 用户名或密码错误 |
| `TOKEN_EXPIRED` | Token 已过期 |
| `TOKEN_INVALID` | Token 格式错误 |
| `SONG_NOT_FOUND` | 歌曲不存在 |
| `FILE_NOT_FOUND` | 音频文件丢失 |
| `INVALID_STYLE` | 风格枚举值非法 |
| `INVALID_MOOD` | 情绪枚举值非法 |
| `FILE_TOO_LARGE` | 文件超过大小限制 |
| `UNSUPPORTED_FORMAT` | 仅支持 MP3 格式 |
| `FAVORITE_EXISTS` | 已收藏该歌曲 |
| `FAVORITE_NOT_FOUND` | 未收藏该歌曲 |
| `PLAYLIST_NOT_FOUND` | 歌单不存在 |
| `SONG_ALREADY_IN_PLAYLIST` | 歌曲已在歌单中 |
| `FORBIDDEN` | 无权操作此资源 |

---

### 5.4 认证模块

#### 5.4.1 POST /api/auth/register — 注册

```
Content-Type: application/json

Request:
{
  "username": "musiclover",           // 必填，3-50字符，字母数字下划线
  "password": "Abc123456",            // 必填，6-100字符
  "nickname": "音乐爱好者"             // 选填，1-100字符
}
```

**校验规则：**

| 字段 | 规则 |
|------|------|
| username | 必填，3-50位，正则 `^[a-zA-Z0-9_]+$` |
| password | 必填，6-100位 |
| nickname | 选填，默认取 username，1-100位 |

**成功响应 (200)：**
```json
{
  "code": 200,
  "message": "注册成功",
  "data": {
    "id": 1,
    "username": "musiclover",
    "nickname": "音乐爱好者",
    "createdAt": "2026-05-09T12:00:00"
  }
}
```

**错误响应：**
```json
// 409 — 用户名已存在
{ "code": 409, "message": "用户名已被注册", "data": null }

// 400 — 参数校验失败
{ "code": 400, "message": "用户名格式不正确", "data": null }
```

---

#### 5.4.2 POST /api/auth/login — 登录

```
Content-Type: application/json

Request:
{
  "username": "musiclover",
  "password": "Abc123456"
}
```

**成功响应 (200)：**
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "eyJhbGciOiJIUzUxMiJ9.eyJzdWIiOiIxIiwidXNlcm5hbWUiOiJtdXNpY2xvdmVyIiwiZXhwIjoxNzQ3Mzg4ODAwfQ.xXxXxXxXxXxXxXxXxXxXxXxXxXxXxXxXxXxXx",
    "user": {
      "id": 1,
      "username": "musiclover",
      "nickname": "音乐爱好者",
      "avatar": null
    }
  }
}
```

**错误响应：**
```json
// 400 — 参数校验失败
{ "code": 400, "message": "用户名或密码错误", "data": null }
```

**JWT 结构：**
| 字段 | 值 |
|------|-----|
| sub | `"1"` (userId) |
| username | `"musiclover"` |
| iat | 签发时间 |
| exp | 签发时间 + 7天 |

---

#### 5.4.3 GET /api/user/me — 获取当前用户信息

```
Authorization: Bearer eyJ...
```

**成功响应 (200)：**
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "username": "musiclover",
    "nickname": "音乐爱好者",
    "avatar": null,
    "createdAt": "2026-05-09T12:00:00"
  }
}
```

**错误响应：**
```json
// 401 — Token 过期或无效
{ "code": 401, "message": "Token已过期，请重新登录", "data": null }
```

---

### 5.5 歌曲模块

#### 5.5.1 GET /api/songs — 歌曲列表（分页+筛选）

```
无需认证

Query Parameters:
  page     int    页码，默认1，最小1
  size     int    每页条数，默认20，最小1，最大50
  style    string 风格筛选（可选）：rock/pop/classical/electronic/folk/jazz/hiphop/rnb
  mood     string 情绪筛选（可选）：happy/sad/calm/excited/romantic/melancholy
  keyword  string 关键词搜索（可选）：匹配 title 和 artist，LIKE %keyword%
  sort     string 排序方式（可选）：newest(默认) / plays / title

示例: GET /api/songs?page=1&size=20&style=pop&mood=happy&keyword=love&sort=plays
```

**成功响应 (200)：**
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "records": [
      {
        "id": 1,
        "title": "晴天",
        "artist": "周杰伦",
        "coverUrl": "/api/songs/1/cover",
        "duration": 269,
        "style": "pop",
        "mood": "happy",
        "playCount": 12800,
        "isFavorited": true,
        "uploader": {
          "id": 1,
          "nickname": "音乐爱好者"
        },
        "createdAt": "2026-05-09T12:00:00"
      }
    ],
    "total": 50,
    "page": 1,
    "size": 20,
    "pages": 3
  }
}
```

**Song 对象字段说明：**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | long | 歌曲ID |
| title | string | 歌曲标题 |
| artist | string | 艺术家/演唱者 |
| coverUrl | string\|null | 封面图URL，null 则前端显示默认 |
| duration | int | 时长（秒），0 表示未知 |
| style | string | 风格标签 |
| mood | string | 情绪标签 |
| playCount | int | 播放次数 |
| isFavorited | boolean | 当前用户是否已收藏（未登录为 false） |
| uploader.id | long | 上传者ID |
| uploader.nickname | string | 上传者昵称 |
| createdAt | datetime | 上传时间 |

---

#### 5.5.2 GET /api/songs/{id} — 歌曲详情

```
无需认证

示例: GET /api/songs/1
```

**成功响应 (200)：**
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "title": "晴天",
    "artist": "周杰伦",
    "coverUrl": null,
    "duration": 269,
    "style": "pop",
    "mood": "happy",
    "playCount": 12801,
    "isFavorited": false,
    "uploader": {
      "id": 1,
      "nickname": "音乐爱好者"
    },
    "createdAt": "2026-05-09T12:00:00"
  }
}
```

**错误响应：**
```json
// 404
{ "code": 404, "message": "歌曲不存在", "data": null }
```

---

#### 5.5.3 GET /api/songs/{id}/stream — 流式播放

```
无需认证

请求头（浏览器自动发送）:
  Range: bytes=0-

示例: GET /api/songs/1/stream
```

**成功响应 — 有 Range (206)：**
```
HTTP/1.1 206 Partial Content
Content-Type: audio/mpeg
Content-Range: bytes 0-1048575/5242880
Accept-Ranges: bytes
Content-Length: 1048576

[MP3 二进制数据块]
```

**成功响应 — 无 Range (200)：**
```
HTTP/1.1 200 OK
Content-Type: audio/mpeg
Content-Length: 5242880
Accept-Ranges: bytes

[全部 MP3 二进制数据]
```

**后端实现要点：**
```java
// 核心逻辑
public ResponseEntity<ResourceRegion> stream(Long id, String rangeHeader) {
    Song song = songMapper.selectById(id);
    Path filePath = Paths.get(audioDir, song.getFilePath());
    Resource resource = new FileSystemResource(filePath);
    long fileSize = resource.contentLength();

    if (rangeHeader == null) {
        return ResponseEntity.ok()
            .header(HttpHeaders.CONTENT_TYPE, "audio/mpeg")
            .header(HttpHeaders.ACCEPT_RANGES, "bytes")
            .body(resource);
    }

    // 解析 Range: bytes=start-end
    RangeResourceRegionConverter converter = new RangeResourceRegionConverter();
    ResourceRegion region = converter.toRegion(resource, rangeHeader);

    return ResponseEntity.status(HttpStatus.PARTIAL_CONTENT)
        .header(HttpHeaders.CONTENT_TYPE, "audio/mpeg")
        .contentLength(region.getCount())
        .body(region);
}
```

**错误响应：**
```json
// 404 — 歌曲不存在
{ "code": 404, "message": "歌曲不存在", "data": null }

// 404 — 文件丢失
{ "code": 404, "message": "音频文件不存在", "data": null }
```

**注意：** 流式接口返回的是 MP3 二进制数据，响应体格式不是 JSON，错误时返回 JSON。前端 `<audio>` 标签遇到 400/404 时需监听 `error` 事件做降级处理。

---

#### 5.5.4 POST /api/songs/upload — 上传 MP3

```
Authorization: Bearer eyJ...
Content-Type: multipart/form-data

Form Fields:
  title   string  必填，1-200字符
  artist  string  必填，1-200字符
  style   string  必填，枚举值：rock/pop/classical/electronic/folk/jazz/hiphop/rnb
  mood    string  必填，枚举值：happy/sad/calm/excited/romantic/melancholy
  audio   file    必填，MP3 文件，Content-Type: audio/mpeg，最大 50MB

示例（curl）:
curl -X POST http://localhost:8080/api/songs/upload \
  -H "Authorization: Bearer eyJ..." \
  -F "title=晴天" \
  -F "artist=周杰伦" \
  -F "style=pop" \
  -F "mood=happy" \
  -F "audio=@song.mp3"
```

**校验规则：**

| 字段 | 规则 |
|------|------|
| title | 必填，1-200 字符，去除首尾空格 |
| artist | 必填，1-200 字符，去除首尾空格 |
| style | 必填，必须在 8 个枚举值中 |
| mood | 必填，必须在 6 个枚举值中 |
| audio | 必填，Content-Type 必须为 `audio/mpeg`，大小 ≤ 50MB |

**成功响应 (200)：**
```json
{
  "code": 200,
  "message": "上传成功",
  "data": {
    "id": 2,
    "title": "晴天",
    "artist": "周杰伦",
    "coverUrl": null,
    "duration": 269,
    "style": "pop",
    "mood": "happy",
    "playCount": 0,
    "uploader": {
      "id": 1,
      "nickname": "音乐爱好者"
    },
    "createdAt": "2026-05-09T14:30:00"
  }
}
```

**错误响应：**
```json
// 400 — 文件格式错误
{ "code": 400, "message": "仅支持 MP3 格式", "data": null }

// 400 — 文件过大
{ "code": 400, "message": "文件大小不能超过 50MB", "data": null }

// 400 — style/mood 枚举值非法
{ "code": 400, "message": "风格值非法，允许的值: rock, pop, classical, electronic, folk, jazz, hiphop, rnb", "data": null }

// 400 — 缺少必填字段
{ "code": 400, "message": "歌曲标题不能为空", "data": null }

// 401 — 未登录
{ "code": 401, "message": "请先登录", "data": null }
```

**后端处理流程：**
```
1. 校验 audio 文件 Content-Type
2. 校验 style/mood 枚举值
3. 生成 UUID 文件名: uploads/audio/{uuid}.mp3
4. Files.copy() 写入磁盘
5. 解析 MP3 获取时长（JAudioTagger）
6. 构建 Song 实体
7. songMapper.insert(song)
8. 返回 Song 响应
@Transactional — 文件写入失败不写数据库
```

---

#### 5.5.5 POST /api/songs/{id}/cover — 上传封面图

```
Authorization: Bearer eyJ...
Content-Type: multipart/form-data

Form Fields:
  cover  file  必填，图片文件，支持 jpg/jpeg/png/webp，最大 5MB

示例: POST /api/songs/1/cover
```

**成功响应 (200)：**
```json
{
  "code": 200,
  "message": "封面上传成功",
  "data": {
    "coverUrl": "/uploads/covers/a1b2c3d4.jpg"
  }
}
```

**错误响应：**
```json
// 400 — 格式错误
{ "code": 400, "message": "仅支持 JPG、PNG、WebP 格式", "data": null }

// 403 — 非上传者
{ "code": 403, "message": "无权修改此歌曲", "data": null }
```

---

#### 5.5.6 GET /api/songs/{id}/cover — 获取封面图

```
无需认证

响应: 图片二进制数据，Content-Type 为实际图片类型
若未设置封面，返回默认封面
```

---

#### 5.5.7 DELETE /api/songs/{id} — 删除歌曲

```
Authorization: Bearer eyJ...
仅上传者可删除
```

**成功响应 (200)：**
```json
{ "code": 200, "message": "删除成功", "data": null }
```

**错误响应：**
```json
// 403 — 非上传者
{ "code": 403, "message": "无权删除此歌曲", "data": null }

// 404
{ "code": 404, "message": "歌曲不存在", "data": null }
```

**后端需同时：** 删除磁盘文件 + 删除数据库记录 + 级联删除关联的收藏/歌单记录

---

### 5.6 收藏模块

#### 5.6.1 POST /api/favorites/{songId} — 收藏歌曲

```
Authorization: Bearer eyJ...
```

**成功响应 (200)：**
```json
{ "code": 200, "message": "收藏成功", "data": null }
```

**错误响应：**
```json
// 409 — 已收藏
{ "code": 409, "message": "已收藏过该歌曲", "data": null }

// 404
{ "code": 404, "message": "歌曲不存在", "data": null }

// 401
{ "code": 401, "message": "请先登录", "data": null }
```

---

#### 5.6.2 DELETE /api/favorites/{songId} — 取消收藏

```
Authorization: Bearer eyJ...
```

**成功响应 (200)：**
```json
{ "code": 200, "message": "已取消收藏", "data": null }
```

**错误响应：**
```json
// 404
{ "code": 404, "message": "未收藏该歌曲", "data": null }
```

---

#### 5.6.3 GET /api/favorites — 收藏列表

```
Authorization: Bearer eyJ...

Query Parameters:
  page  int  默认1
  size  int  默认20，最大50
```

**成功响应 (200)：**
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "records": [
      {
        "id": 1,
        "title": "晴天",
        "artist": "周杰伦",
        "coverUrl": null,
        "duration": 269,
        "style": "pop",
        "mood": "happy",
        "playCount": 12801,
        "isFavorited": true,
        "favoritedAt": "2026-05-09T15:00:00",
        "uploader": {
          "id": 2,
          "nickname": "上传者"
        },
        "createdAt": "2026-05-09T12:00:00"
      }
    ],
    "total": 15,
    "page": 1,
    "size": 20,
    "pages": 1
  }
}
```

**额外字段：`favoritedAt`** — 收藏时间

---

#### 5.6.4 GET /api/favorites/check/{songId} — 检查收藏状态

```
Authorization: Bearer eyJ...
```

**成功响应 (200)：**
```json
{ "code": 200, "message": "success", "data": { "favorited": true } }
```

```json
{ "code": 200, "message": "success", "data": { "favorited": false } }
```

---

### 5.7 歌单模块

#### 5.7.1 POST /api/playlists — 创建歌单

```
Authorization: Bearer eyJ...
Content-Type: application/json

Request:
{
  "name": "我的最爱",            // 必填，1-200字符
  "description": "那些单曲循环的日子", // 选填，最多500字符
  "isPublic": true              // 选填，默认 true
}
```

**成功响应 (200)：**
```json
{
  "code": 200,
  "message": "创建成功",
  "data": {
    "id": 1,
    "name": "我的最爱",
    "description": "那些单曲循环的日子",
    "cover": null,
    "isPublic": true,
    "songCount": 0,
    "owner": {
      "id": 1,
      "nickname": "音乐爱好者"
    },
    "createdAt": "2026-05-09T16:00:00"
  }
}
```

---

#### 5.7.2 GET /api/playlists — 我的歌单列表

```
Authorization: Bearer eyJ...
```

**成功响应 (200)：**
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "name": "我的最爱",
      "cover": null,
      "isPublic": true,
      "songCount": 12,
      "owner": { "id": 1, "nickname": "音乐爱好者" },
      "createdAt": "2026-05-09T16:00:00"
    }
  ]
}
```

---

#### 5.7.3 GET /api/playlists/{id} — 歌单详情（含歌曲列表）

```
无需认证（公开歌单可访问）

返回歌单信息 + 歌曲列表（Song[]，按 sort_order 升序排列）
```

**成功响应 (200)：**
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "name": "我的最爱",
    "description": "那些单曲循环的日子",
    "isPublic": true,
    "owner": { "id": 1, "nickname": "音乐爱好者" },
    "songs": [
      {
        "id": 1,
        "title": "晴天",
        "artist": "周杰伦",
        "coverUrl": null,
        "duration": 269,
        "style": "pop",
        "mood": "happy",
        "playCount": 12801,
        "isFavorited": false,
        "sortOrder": 1,
        "uploader": { "id": 2, "nickname": "上传者" },
        "createdAt": "2026-05-09T12:00:00"
      }
    ],
    "songCount": 1,
    "createdAt": "2026-05-09T16:00:00"
  }
}
```

**私密歌单非 owner 访问：**
```json
// 403
{ "code": 403, "message": "无权访问此歌单", "data": null }
```

---

#### 5.7.4 PUT /api/playlists/{id} — 更新歌单信息

```
Authorization: Bearer eyJ...
仅创建者可编辑

Request:
{
  "name": "新的歌单名",         // 选填
  "description": "更新后的描述", // 选填
  "isPublic": false            // 选填
}
```

**成功响应 (200)：**
```json
{ "code": 200, "message": "更新成功", "data": { 完整歌单对象 } }
```

---

#### 5.7.5 DELETE /api/playlists/{id} — 删除歌单

```
Authorization: Bearer eyJ...
仅创建者可删除

级联删除 playlist_songs 关联记录，不删除歌曲本身
```

**成功响应 (200)：**
```json
{ "code": 200, "message": "删除成功", "data": null }
```

---

#### 5.7.6 POST /api/playlists/{id}/songs — 添加歌曲到歌单

```
Authorization: Bearer eyJ...
Content-Type: application/json

Request:
{
  "songId": 1
}

sort_order 自动设为当前歌单中最大 sort_order + 1
```

**成功响应 (200)：**
```json
{ "code": 200, "message": "添加成功", "data": null }
```

**错误响应：**
```json
// 409
{ "code": 409, "message": "歌曲已在歌单中", "data": null }

// 403
{ "code": 403, "message": "无权编辑此歌单", "data": null }
```

---

#### 5.7.7 DELETE /api/playlists/{id}/songs/{songId} — 从歌单移除歌曲

```
Authorization: Bearer eyJ...
```

**成功响应 (200)：**
```json
{ "code": 200, "message": "移除成功", "data": null }
```

---

### 5.8 播放历史模块

#### 5.8.1 POST /api/history/{songId} — 记录播放

```
Authorization: Bearer eyJ...

注：此接口无 Request Body，songId 从路径获取，服务器时间作为 playedAt
     每次调用新增一条记录（不合并去重），同时递增歌曲 play_count
```

**成功响应 (200)：**
```json
{ "code": 200, "message": "记录成功", "data": null }
```

**错误响应：**
```json
// 404
{ "code": 404, "message": "歌曲不存在", "data": null }
```

---

#### 5.8.2 GET /api/history — 播放历史列表

```
Authorization: Bearer eyJ...

Query Parameters:
  page  int  默认1
  size  int  默认20，最大50

按 played_at 降序排列（最近播放排前）
```

**成功响应 (200)：**
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "records": [
      {
        "id": 1,
        "song": {
          "id": 1,
          "title": "晴天",
          "artist": "周杰伦",
          "coverUrl": null,
          "duration": 269,
          "style": "pop",
          "mood": "happy",
          "playCount": 12801,
          "isFavorited": false,
          "uploader": { "id": 2, "nickname": "上传者" }
        },
        "playedAt": "2026-05-09T18:30:00"
      }
    ],
    "total": 80,
    "page": 1,
    "size": 20,
    "pages": 4
  }
}
```

**历史记录对象字段：**

| 字段 | 类型 | 说明 |
|------|------|------|
| id | long | 历史记录ID |
| song | Song | 歌曲完整信息 |
| playedAt | datetime | 播放时间 |

---

### 5.9 接口汇总

| 模块 | 方法 | 路径 | 认证 |
|------|------|------|------|
| 认证 | POST | `/api/auth/register` | 否 |
| 认证 | POST | `/api/auth/login` | 否 |
| 认证 | GET | `/api/user/me` | 是 |
| 歌曲 | GET | `/api/songs` | 否 |
| 歌曲 | GET | `/api/songs/{id}` | 否 |
| 歌曲 | GET | `/api/songs/{id}/stream` | 否 |
| 歌曲 | GET | `/api/songs/{id}/cover` | 否 |
| 歌曲 | POST | `/api/songs/upload` | 是 |
| 歌曲 | POST | `/api/songs/{id}/cover` | 是 |
| 歌曲 | DELETE | `/api/songs/{id}` | 是 |
| 收藏 | POST | `/api/favorites/{songId}` | 是 |
| 收藏 | DELETE | `/api/favorites/{songId}` | 是 |
| 收藏 | GET | `/api/favorites` | 是 |
| 收藏 | GET | `/api/favorites/check/{songId}` | 是 |
| 歌单 | POST | `/api/playlists` | 是 |
| 歌单 | GET | `/api/playlists` | 是 |
| 歌单 | GET | `/api/playlists/{id}` | 否 |
| 歌单 | PUT | `/api/playlists/{id}` | 是 |
| 歌单 | DELETE | `/api/playlists/{id}` | 是 |
| 歌单 | POST | `/api/playlists/{id}/songs` | 是 |
| 歌单 | DELETE | `/api/playlists/{id}/songs/{songId}` | 是 |
| 历史 | POST | `/api/history/{songId}` | 是 |
| 历史 | GET | `/api/history` | 是 |

---

## 6. 后端核心配置

### 6.1 pom.xml 关键依赖

```xml
<parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.5.14</version>
</parent>

<properties>
    <java.version>17</java.version>
</properties>

<dependencies>
    <!-- Spring Boot Starters -->
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-security</artifactId>
    </dependency>
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>

    <!-- MyBatis -->
    <dependency>
        <groupId>org.mybatis.spring.boot</groupId>
        <artifactId>mybatis-spring-boot-starter</artifactId>
        <version>3.0.3</version>
    </dependency>

    <!-- MySQL -->
    <dependency>
        <groupId>com.mysql</groupId>
        <artifactId>mysql-connector-j</artifactId>
        <scope>runtime</scope>
    </dependency>

    <!-- JWT -->
    <dependency>
        <groupId>io.jsonwebtoken</groupId>
        <artifactId>jjwt-api</artifactId>
        <version>0.12.5</version>
    </dependency>
    <dependency>
        <groupId>io.jsonwebtoken</groupId>
        <artifactId>jjwt-impl</artifactId>
        <version>0.12.5</version>
        <scope>runtime</scope>
    </dependency>
    <dependency>
        <groupId>io.jsonwebtoken</groupId>
        <artifactId>jjwt-jackson</artifactId>
        <version>0.12.5</version>
        <scope>runtime</scope>
    </dependency>

    <!-- Lombok -->
    <dependency>
        <groupId>org.projectlombok</groupId>
        <artifactId>lombok</artifactId>
        <optional>true</optional>
    </dependency>

    <!-- Test -->
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-test</artifactId>
        <scope>test</scope>
    </dependency>
</dependencies>
```

### 6.2 application.yml

```yaml
server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/music_platform?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: your_password
    driver-class-name: com.mysql.cj.jdbc.Driver

  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.musicplatform.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

# JWT 配置
jwt:
  secret: YourSuperSecretKeyForJWTGenerationMin256Bits
  expiration: 604800000  # 7天 (毫秒)

# 文件上传路径
app:
  upload:
    audio-dir: ../uploads/audio/
    cover-dir: ../uploads/covers/
```

### 6.3 JWT 核心实现要点

```java
// JwtTokenProvider.java — 关键方法
public String generateToken(Long userId, String username) {
    return Jwts.builder()
        .subject(String.valueOf(userId))
        .claim("username", username)
        .issuedAt(new Date())
        .expiration(new Date(System.currentTimeMillis() + expirationMs))
        .signWith(secretKey)
        .compact();
}

public Long getUserIdFromToken(String token) {
    return Long.parseLong(
        Jwts.parser().verifyWith(secretKey).build()
            .parseSignedClaims(token).getPayload().getSubject()
    );
}
```

### 6.4 SecurityConfig 核心结构

```java
@Configuration
@EnableWebSecurity
public class SecurityConfig {

    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) {
        http
            .csrf(AbstractHttpConfigurer::disable)
            .cors(withDefaults())
            .sessionManagement(sm -> sm.sessionCreationPolicy(STATELESS))
            .authorizeHttpRequests(auth -> auth
                .requestMatchers("/api/auth/**").permitAll()
                .requestMatchers(HttpMethod.GET, "/api/songs/**").permitAll()
                .requestMatchers("/api/songs/*/stream").permitAll()
                .anyRequest().authenticated()
            )
            .addFilterBefore(jwtFilter, UsernamePasswordAuthenticationFilter.class);
        return http.build();
    }
}
```

---

## 7. 前端架构设计

### 7.1 路由设计

| 路径 | 页面 | 认证 | 说明 |
|------|------|------|------|
| `/login` | LoginPage | 否 | 登录/注册切换表单 |
| `/` | HomePage（音乐库） | 否 | 3D 粒子隧道首页 |
| `/play/:songId` | PlayPage（全屏播放 + 3D 场景） | 否 | 城市 3D 场景 + 灯塔 + 雪花 |
| `/upload` | UploadPage | 是 | 赛博朋克 ECG 上传页 |
| `/profile` | ProfilePage | 是 | 4 标签页：歌单/上传/历史/收藏 |

注：注册页已合并到 LoginPage；歌单列表/详情/收藏/历史页面已合并到 ProfilePage 的标签页中。

### 7.2 状态管理（Zustand）

共 5 个 store：

```typescript
// stores/playerStore.ts
interface PlayerState {
  queue: Song[];
  currentIndex: number;
  isPlaying: boolean;
  currentTime: number;
  duration: number;
  volume: number;       // 0-1
  playMode: 'sequential' | 'shuffle' | 'repeat-one';

  // 计算属性
  currentSong: () => Song | null;

  // 操作
  play: (song: Song, queue?: Song[]) => void;
  pause: () => void;
  resume: () => void;
  next: () => void;
  prev: () => void;
  seek: (time: number) => void;
  setVolume: (vol: number) => void;
  setDuration: (dur: number) => void;
  setCurrentTime: (time: number) => void;
  togglePlayMode: () => void;
  recordHistory: () => void;  // 调用 historyApi.record()
}

// stores/authStore.ts
interface AuthState {
  user: User | null;
  token: string | null;
  isLoggedIn: boolean;
  login: (username: string, password: string) => Promise<void>;
  register: (username: string, password: string, nickname?: string) => Promise<void>;
  logout: () => void;
  fetchMe: () => Promise<void>;
}

// stores/useStore.ts — 首页全局状态
interface AppState {
  mouseX: number;           // 鼠标 X 坐标（影响 3D 摄像机旋转）
  mouseY: number;
  currentSpeed: number;      // 粒子当前速度（lerp 平滑过渡）
  targetSpeed: number;       // 粒子目标速度（场景切换时改变）
  accentColor: string;       // 当前强调色（场景切换时改变）
  isShockwave: boolean;      // 触发冲击波动画
  activeScene: string;       // 当前场景 key
  // + setters
}

// stores/useUploadStore.ts — 上传页状态
interface UploadState {
  isUploading: boolean;
  uploadProgress: number;     // 0-1
  selectedFile: File | null;
  title: string;
  artist: string;
  style: StyleType | null;
  mood: MoodType | null;
  error: string;
  // + setters + startUpload / finishUpload / reset
}

// stores/useMouseStore.ts — 登录页鼠标跟踪（3D 视差）
interface MouseState {
  x: number;  // 归一化 -1 ~ 1
  y: number;
  setMouse: (x: number, y: number) => void;
}
```

### 7.3 Axios 封装

```typescript
// api/client.ts
import axios from 'axios';

const client = axios.create({
  baseURL: '/api',
  timeout: 30000,
});

// 请求拦截器 — 注入 JWT
client.interceptors.request.use((config) => {
  const token = localStorage.getItem('token');
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});

// 响应拦截器 — 401 跳转登录
client.interceptors.response.use(
  (res) => res.data,
  (err) => {
    if (err.response?.status === 401) {
      localStorage.removeItem('token');
      window.location.href = '/login';
    }
    return Promise.reject(err);
  }
);

export default client;
```

### 7.4 样式架构

项目摒弃了 Tailwind CSS，采用**手写 CSS** 方案（`src/index.css` 500+ 行 + 页面独立样式文件）。

**全局工具类（`index.css`）：**
- `.glass-panel` — 玻璃态面板（背景模糊 + 半透明边框 + 内阴影）
- `.glass-btn` — 圆形玻璃态按钮（hover 发光）
- `.btn-neon` / `.btn-disabled` — 霓虹渐变按钮
- `.neon-text` — 霓虹文字发光
- `.neon-progress` — 霓虹渐变进度条（`#FF8C42 → #FFB366 → #FFE0C0`）
- `.bg-white-2` ~ `.bg-white-10` — 白色透明度梯级（2%~10%）
- `.bg-accent-5` / `.bg-accent-15` — 强调色透明度背景
- `.border-white-6` / `.border-white-8` / `.border-white-10` — 半透明边框
- `.text-primary` / `.text-secondary` — 文字颜色变量
- `.animate-spin` / `.animate-pulse` / `.animate-livePulse` — 动画
- `.shockwave` — 场景切换冲击波 CSS 动画
- 响应式断点：`@media (max-width: 768px)` / `(max-width: 480px)`

**页面独立样式：**
- `src/styles/login.css` — 登录页暗霓虹粉金主题（#FF007F / #FFD700）
- `src/styles/home.css` — 首页样式
- `src/styles/upload.css` — 上传页赛博朋克绿色主题（#00ff9d）

**组件样式：** 大部分使用内联样式（`style={}` 对象）确保可靠渲染，避免 CSS 类加载问题。

---

## 8. 3D 场景引擎架构

### 8.1 整体架构

3D 场景按页面独立实现，非集中式路由：

| 页面 | 场景组件 | 说明 |
|------|---------|------|
| LoginPage | `Scene3D.tsx` + `RomanticShape.tsx` | 鼠标视差摄像机 + 粉金环形结 |
| HomePage | `HomeScene3D.tsx` + `ParticleTunnel.tsx` | 6000 粒子圆柱隧道，速度/颜色动态 |
| PlayPage | PlayPage 内联 | 城市 + 灯塔 + 粒子 + 雪花（全部在 PlayPage.tsx 内） |
| ProfilePage | `CityScene.tsx` | 抽象旋转几何体 + 网格地面 + 星空 |
| 通用 | `StarryGalaxy.tsx`, `SceneBanner.tsx`, `LoginBackground.tsx` | 可复用简单场景 |

```
PlayPage.tsx 3D 场景结构:
├── <Canvas>
│   ├── <SkyDome />          # 自定义 ShaderMaterial 日出渐变天空
│   ├── <Ground />           # Canvas 生成道路纹理（2048×2048）
│   ├── <Buildings />        # 5 种建筑类型（低层/多层/玻璃商业/塔楼/公园）
│   │   ├── 窗户灯光（暖黄色 emissive）
│   │   └── 路灯（沿主干道）
│   ├── <Lighthouse />       # 多层石基 + 3 段玻璃塔 + 旋转光束锥
│   │   ├── 2 个点光源
│   │   └── 基座发光环
│   ├── <Particles />        # 250 尘埃 + 20 萤火虫 + 70 光束粒子 + 120 闪烁粒子
│   ├── <SnowParticles />    # 400 白色雪花（120m 高空飘落 + 正弦风漂移）
│   ├── <Stars />            # 300 星星
│   ├── <OrbitControls />    # 目标 [-30, -5, -20]，30~350 缩放范围
│   ├── <ZoomController />   # 仅 zoomLevel 变化时更新 camera.position.z
│   └── 环境光 (0.8) + 半球光 (0.7) + 方向光 + 暖色雾 (#e07840)
```

### 8.2 当前场景实现状态

已实现场景（4 个核心场景 + 3 个辅助）：

| 场景 Key | 组件 | 状态 |
|----------|------|------|
| city | `CityScene.tsx` | 抽象几何体城市，用于 Profile 和 SceneEngine 默认 |
| galaxy | `StarryGalaxy.tsx` | 旋转星群 + 紫色发光球体 |
| romantic | `Scene3D.tsx` + `RomanticShape.tsx` | 登录页专用，粉金 TorusKnot |
| particle-tunnel | `HomeScene3D.tsx` + `ParticleTunnel.tsx` | 首页专用 6000 粒子隧道 |
| banner | `SceneBanner.tsx` | 场景横幅展示 |
| background | `LoginBackground.tsx` | 简单星空背景 |

未实现场景（12 个规划中）：cyberpunk, mountain, spring, beach, autumn, winter, neon, rain, desert, palace 等——后续可按需扩展。

### 8.3 SceneEngine 组件

```typescript
// components/scenes/SceneEngine.tsx
// 当前为简化实现，始终返回 CityScene
// 后续可扩展为根据 style+mood 路由不同场景
export function SceneEngine({ style, mood, lockedScene }: Props) {
  return <CityScene />;
}
```

### 8.4 场景过渡

页面间过渡使用 Framer Motion `AnimatePresence`（在 App.tsx 中），淡入淡出 0.3s。PlayPage 内场景切换无过渡动画（当前版本不支持 style/mood 自动路由场景切换）。

---

## 9. 播放器 UI 布局（全屏播放页）

参考原型：`preview/india-radio.html` — 3D 沉浸式音乐电台布局

### 9.1 布局总览

```
┌──────────────────────────────────────────────────────────────┐
│ 🔊 ♡ ⚙                                                     │  ← 顶部图标栏
│                                                              │
│                                                              │
│                    ┌──────────────────┐          ┌────┐      │
│                    │  ┌────────────┐  │          │ □  │      │
│                    │  │  城市天际线  │  │          ├────┤      │
│                    │  │  [LIVE] [×] │  │          │ +  │      │  ← 右侧控制栏
│                    │  ├────────────┤  │  3D场景  ├────┤      │
│                    │  │  歌曲标题   │  │  区域     │ -  │      │
│                    │  │  🎸 摇滚   │  │          ├────┤      │
│                    │  │  艺术家名   │  │          │ ⛶  │      │
│                    │  │ 👁0 👍💬⭐ │  │          └────┘      │
│                    │  │ [停止收听]  │  │                      │
│                    │  └────────────┘  │                      │
│                    │  左侧电台面板     │                      │
│                    │  (310px)         │                      │
│                    └──────────────────┘                      │
│                                                              │
│ ┌──────────────────────────────────────────────────────────┐ │
│ │ (💿) 歌曲标题                   0:00 ━━━━━━ 3:42 🔊  ▶  │ │  ← 底部播放器栏
│ │      艺术家                      ⏮ 🔁 ⏸ ⏭               │ │
│ └──────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────┘
```

### 9.2 五大 UI 区域

| 区域 | 位置 | 组件 | 规格 |
|------|------|------|------|
| **顶部图标栏** | `top: 28px, left: 28px` | TopIcons | 3 个圆形按钮（40px），gap 10px |
| **右侧控制栏** | `right: 18px, top: 50%` | RightControls | 4 个圆形按钮（34px）白底垂直排列，gap 8px |
| **左侧电台面板** | `left: 28px, top: 45%` | RadioPanel | 310×auto，玻璃态卡片 |
| **底部播放器栏** | `bottom: 0, left: 0, right: 0` | PlayerBar | 72px 高，玻璃态底栏，全部内联样式 |

注：右上角品牌文字 "HEAR THE 🌍" 已移除，简化布局。

### 9.3 顶部图标栏

```
位置: fixed, top: 28px, left: 28px, z-index: 10

┌────┐ ┌────┐ ┌────┐
│ 🔊 │ │ ♡  │ │ ⚙  │    ← 3个圆形玻璃按钮，gap: 10px
└────┘ └────┘ └────┘

按钮规格:
  width/height: 40px, border-radius: 50%
  background: rgba(20, 16, 28, 0.45)
  backdrop-filter: blur(14px)
  border: 1px solid rgba(255, 180, 130, 0.08)
  color: #b0a090, font-size: 17px
  hover → bg加深, border变亮, 暖色光晕
```

对应组件：`TopIcons.tsx`
```tsx
// 三个按钮: 音量控制 / 喜欢 / 设置
<TopIcons>
  <IconBtn icon="🔊" onClick={toggleMute} />
  <IconBtn icon={isFavorited ? "❤️" : "♡"} onClick={toggleFavorite} />
  <IconBtn icon="⚙" onClick={openSettings} />
</TopIcons>
```

### 9.4 右侧控制栏

```
位置: fixed, right: 18px, top: 50%, translateY(-50%), z-index: 10

┌────┐
│ □  │  ← 展开/收起面板
├────┤
│ +  │  ← 放大场景
├────┤
│ -  │  ← 缩小场景
├────┤
│ ⛶  │  ← 全屏切换
└────┘

按钮规格:
  width/height: 34px, border-radius: 50%
  background: #f0e6e0（白底实体，暗色页面中清晰可见）
  color: #1a1428, font-size: 14px
  垂直排列, gap: 8px
```

对应组件：`RightControls.tsx`
```tsx
<RightControls>
  <CtrlBtn icon="□" onClick={togglePanel} />
  <CtrlBtn icon="+" onClick={zoomIn} />
  <CtrlBtn icon="−" onClick={zoomOut} />
  <CtrlBtn icon="⛶" onClick={toggleFullscreen} />
</RightControls>
```

### 9.5 左侧电台面板

```
位置: fixed, left: 28px, top: 45%, translateY(-55%), z-index: 10

┌──────────────────────────────┐
│ ┌──────────────────────────┐ │
│ │    城市天际线剪影          │ │  ← 缩略图区 (160px)
│ │    [×]          [LIVE]   │ │    LIVE标签: 红色脉冲动画
│ └──────────────────────────┘ │
│                              │
│  歌曲标题 (20px bold)         │
│  🎸 摇滚 · 💕 浪漫          │  ← style + mood 标签
│  艺术家名（斜体半透明）        │
│                              │
│  👁 12,800  👍 48  💬 12  ⭐│  ← 统计栏
│                              │
│  ┌────────────────────────┐  │
│  │  ■  停止收听            │  │  ← 停止/继续切换（红/绿渐变）
│  └────────────────────────┘  │
└──────────────────────────────┘

面板规格:
  width: 310px
  background: rgba(18, 14, 24, 0.55)
  backdrop-filter: blur(28px)
  border: 1px solid rgba(255, 255, 255, 0.08)
  border-radius: 16px
  box-shadow: 0 24px 70px rgba(0,0,0,0.55), inset 0 1px 0 rgba(255,200,150,0.04)
```

**停止按钮状态切换：**
| 状态 | 文字 | 背景色 |
|------|------|--------|
| 播放中 | 停止收听 | 红色渐变 `#c0392b → #a01d1d` |
| 已停止 | 继续收听 | 绿色渐变 `#33aa55 → #228844` |

### 9.6 底部播放器栏

```
位置: fixed, bottom: 0, left: 0, right: 0, z-index: 50

┌──────────────────────────────────────────────────────────────────┐
│  ┌────┐  歌曲标题              0:00 ━━━━━━━━━━ 3:42 🔊 ⏮ 🔁 ▶ ⏭ │
│  │ 💿 │  艺术家                                               │
│  └────┘                                                       │
│  46px唱片    歌曲信息              播放控制区（内联样式）         │
└──────────────────────────────────────────────────────────────────┘

底栏规格（全部内联样式，不依赖 CSS 类）:
  height: 72px
  background: rgba(14, 12, 20, 0.8)
  backdrop-filter: blur(24px)
  border-top: 1px solid rgba(255, 255, 255, 0.06)
  padding: 0 20px, gap: 14px
  display: flex, align-items: center
```

**实际内联样式实现（`PlayerBar.tsx`）：**

| 元素 | 规格 |
|------|------|
| 唱片 | 46px 圆形，渐变背景，旋转动画（播放中 `spin 8s linear infinite`） |
| 歌名 | 13px，`#f0e6e0`，单行省略 |
| 艺术家 | 10px，`#a09080`，单行省略 |
| 时间 | 10px monospace，`#a09080` |
| 进度条 | 220px 宽，4px 高，霓虹渐变填充 `.neon-progress` |
| 音量轨道 | 80px 宽，3px 高，拖拽调整 |
| 控制按钮 | 34px 圆形，透明背景，`#b0a090` |
| 播放按钮 | 44px 圆形，`#f0e6e0` 白底实体，`#1a1428` 深色图标 |
| 模式图标 | 🔁 顺序 / 🔀 随机 / 🔂 单曲循环 |

**组件简化说明：**
- 歌曲信息仅显示 title + artist（无 VIP 标签、无来源行）
- 所有样式使用内联 `style={}` 对象，确保可靠渲染
- 音量滑块支持鼠标拖拽（mousedown/mousemove/mouseup）
- 进度条点击跳转（根据点击位置计算时间）
- 点击唱片或歌曲信息导航到 `/play/:songId`

### 9.7 全局播放器栏

`PlayerBar.tsx` 是全局组件，通过 `AppLayout.tsx` 渲染在所有页面底部（除 LoginPage）。通过 `useAudioPlayer` hook 管理 `<audio>` 元素生命周期：

```tsx
// AppLayout.tsx
<AppLayout>
  <Outlet />       {/* 页面内容 */}
  <PlayerBar />    {/* 全局底部播放器栏 — 无歌曲时返回 null */}
</AppLayout>
```

### 9.8 响应式断点

| 断点 | 调整 |
|------|------|
| **≤768px** | 面板宽 260px, 底栏高 68px, 封面 42px, 歌名 12px, 进度条 150px, 音量 55px |
| **≤480px** | 面板宽 230px, 缩略图高 110px, 标题 16px, 控制按钮 28px, 顶部图标 32px |

---

## 10. 核心交互流程

### 10.1 播放一首歌的完整数据流

```
用户点击歌曲卡片
    ↓
playerStore.play(song, queue)
    ↓
更新 currentSong, queue, isPlaying: true
    ↓
PlayPage 检测 currentSong 变化
    ↓
PlayPage 根据 currentSong 更新 UI（注：当前版本 SceneEngine 返回固定 CityScene，未按 style/mood 路由）
    ↓
<audio> src 指向 GET /api/songs/{id}/stream
    ↓
浏览器自动发送 Range 请求 → Spring Boot 返回 206 Partial Content
    ↓
音频开始播放，场景粒子动画启动
    ↓
每 250ms 更新 playerStore.currentTime
    ↓
POST /api/history/{songId} 记录播放
```

### 10.2 上传一首歌的流程

```
UploadPage
    ↓
用户拖入 MP3 文件 + 选择 style/mood + 填写 title/artist
    ↓
POST /api/songs/upload (multipart/form-data)
    ↓
后端:
  1. 接收 MultipartFile
  2. 保存到 uploads/audio/{uuid}.mp3
  3. 用 JAudioTagger 或 FFmpeg 获取时长
  4. 写入 songs 表
  5. 返回 Song 对象
    ↓
前端跳转到首页，新歌曲出现在列表中
```

---

## 11. 分阶段开发计划

### 第一阶段（后端基础）— 预计 2 天

| 任务 | 产出 |
|------|------|
| Spring Boot 项目初始化 | pom.xml, application.yml, 启动类 |
| 数据库建表 | schema.sql 执行 |
| MyBatis 配置 | mapper XML, 实体类, 分页插件 |
| 用户认证 | register/login API, JWT 过滤器, SecurityConfig |
| 统一响应体 | ApiResponse, GlobalExceptionHandler |

### 第二阶段（歌曲 API）— 预计 1.5 天

| 任务 | 产出 |
|------|------|
| 歌曲上传 | multipart 接收, 文件存储, 时长获取 |
| 歌曲列表 | 分页查询, style/mood/关键词筛选 |
| 流式播放 | Range 请求支持 |
| 封面上传 | 图片存储 + 路径更新 |

### 第三阶段（收藏 + 歌单 + 历史 API）— 预计 1 天

| 任务 | 产出 |
|------|------|
| 收藏 CRUD | 收藏/取消/列表/检查 |
| 歌单 CRUD | 创建/编辑/删除/添加歌曲/移除歌曲 |
| 播放历史 | 记录 + 列表查询 |

### 第四阶段（前端基础）✅ 已完成

| 任务 | 产出 | 状态 |
|------|------|------|
| Vite 项目创建 | package.json, vite.config, React 19, TypeScript | ✅ |
| CSS 方案 | 手写 CSS（index.css 500+ 行 + 页面独立样式），废弃 Tailwind | ✅ |
| 路由 + 布局 | AppLayout, PlayerBar, 5 个路由（lazy-loaded + AnimatePresence） | ✅ |
| Axios 封装 | 拦截器, JWT 注入, 401 自动跳转 | ✅ |
| Zustand stores | authStore, playerStore, useStore, useUploadStore, useMouseStore 共 5 个 | ✅ |
| 登录/注册页面 | 3D 浪漫场景 + 玻璃态表单（注册合并到同一页面） | ✅ |

### 第五阶段（核心页面）✅ 已完成

| 任务 | 产出 | 状态 |
|------|------|------|
| 音乐库首页 | 3D 粒子隧道 + 场景选择器 + 音乐列表 | ✅ |
| 上传页面 | 赛博朋克风格 + 拖拽 + ECG 监视器 | ✅ |
| 播放页面 | 全屏 3D 城市场景 + 灯塔 + 雪花 + 灯塔光束粒子 | ✅ |
| 个人主页 | 4 标签页：歌单/上传/历史/收藏 + 编辑资料/创建歌单弹窗 | ✅ |
| useAudioPlayer Hook | `<audio>` 封装, 自动下一曲, 2s 延迟记录历史 | ✅ |

### 第六阶段（3D 场景）🔄 部分完成

| 任务 | 产出 | 状态 |
|------|------|------|
| react-three-fiber 基础 | Canvas, 灯光, 相机, 雾效, OrbitControls | ✅ |
| SceneEngine 路由 | 已创建骨架，当前返回固定 CityScene | 🔄 |
| 核心场景 | CityScene（抽象城市）, Scene3D（登录浪漫）, HomeScene3D（粒子隧道）, StarryGalaxy | ✅ 4/12 |
| 粒子特效 | 雪花粒子、萤火虫、尘埃、光束粒子、闪烁粒子（均内联在 PlayPage） | ✅ |
| 剩余场景 | cyberpunk, mountain, spring, beach, autumn, winter, neon, rain, desert, palace | ❌ 待开发 |

### 第七阶段（歌单 + 收藏 + 历史页面）✅ 已完成（合并）

所有功能已合并到 ProfilePage 的 4 个标签页中，无需独立页面：
- 收藏标签页：列表 + 取消收藏 + 播放
- 历史标签页：按今天/昨天/更早分组 + 重新播放
- 歌单标签页：列表 + 创建歌单弹窗

### 第八阶段（优化 + 联调）— 预计 1.5 天

| 任务 | 产出 |
|------|------|
| 移动端适配 | 响应式布局 |
| 性能优化 | 代码分割, InstancedMesh, 纹理 mipmap |
| 前后端联调 | 全流程走通 |
| 边缘情况处理 | 空状态, 加载态, 错误态 |

---

## 12. 注意事项

1. **Range 请求**：Spring Boot 需要手动实现 `ResourceRegion` 返回，默认的 `ResourceHttpMessageConverter` 不自动支持 Range
2. **文件路径安全**：MP3 文件存储于 `uploads/` 目录，需在 WebMvcConfig 中配置资源映射，防止路径遍历攻击
3. **场景内存管理**：react-three-fiber Canvas 卸载时需 dispose geometries/materials，避免 WebGL 上下文泄漏
4. **<audio> 自动播放**：浏览器限制自动播放，用户首次需手动点击播放，后续可通过 JS 切换 src
5. **SimplexNoise**：开发文档中已有独立实现，迁移到 TypeScript 时保持算法不变
6. **CORS**：开发时 Vite dev server 在 5173，Spring Boot 在 8080，需配置 CORS 或 Vite 代理
