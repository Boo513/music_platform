<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.musicplatform.mapper.HistoryMapper">

    <insert id="insert">
        INSERT INTO play_history (user_id, song_id) VALUES (#{userId}, #{songId})
    </insert>

    <select id="selectByUserId" resultType="com.musicplatform.entity.PlayHistory">
        SELECT * FROM play_history WHERE user_id = #{userId}
        ORDER BY played_at DESC
        LIMIT #{offset}, #{size}
    </select>

    <select id="countByUserId" resultType="int">
        SELECT COUNT(*) FROM play_history WHERE user_id = #{userId}
    </select>

    <delete id="deleteBySongId">
        DELETE FROM play_history WHERE song_id = #{songId}
    </delete>

</mapper>
